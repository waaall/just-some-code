我现在有一个故障树(绘图软件生成的文件)。在flask框架下，有以下几个功能：

1. 类FaultTree，算是一个struct，是定义故障树数据结构的。比如有leaf_rules(对应rules 和 formatted_rule_mapping)、

2. 类FaultTreeCreate
    - 函数prase_fta（对应函数get_nodes_and_connections、read_and_process_graph、extract_rules）解析文件，分析故障树规则，如果不满足规则，返回给java后端错误；如果满足规则，返回给java后段需要的测点及规则信息。
    - 函数cal_minimum_cutsets（对应函数calculate_minimum_cutsets）
    - 函数store_fta（对应函数store_data_to_clickhouse）不要存到服务器，也不要再服务器读取文件，而是整个树的信息存为一个json文件。
3. 类FaultTreeMonitor
    - 读取FaultTreeCreate.store_fta生成的json文件，